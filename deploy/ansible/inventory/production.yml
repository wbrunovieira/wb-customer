all:
  hosts:
    # Mesmo servidor Contabo que hospeda o WB-crm. Os dois projetos convivem,
    # por isso as portas do wb-customer são exclusivas — ver group_vars/all.yml.
    customer_server:
      ansible_host: 45.90.123.190
      ansible_user: root
      ansible_ssh_private_key_file: ~/.ssh/id_rsa
      ansible_python_interpreter: /usr/bin/python3.9
